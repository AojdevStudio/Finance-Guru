# CI-equivalent checks the review gate runs on a clean tree at HEAD (ci.yml quality job + the pre-commit pytest-coverage command).
uv run ruff check .
uv run ruff format --check .
uv run mypy src/
uv run pytest --cov=src --cov-fail-under=80 --cov-report=term-missing -q -m "not integration"
