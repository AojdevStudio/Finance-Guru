# CI-equivalent checks the review gate runs on a clean tree at HEAD (ci.yml quality job + unit tests).
uv run ruff check .
uv run ruff format --check .
uv run mypy src/
uv run pytest tests/python -q -m "not integration"
